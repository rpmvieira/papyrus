<% if @engajamento.lideranca?(current_usuario) %>

		<%= render partial: "engajamentos/partials/form/contato_novo", locals: { pessoa: @engajado.pessoa} %>
		<%= turbo_stream.update "flash" do %>
			<% if @contato.errors.any? %>
			<div id="error_explanation" class="bg-red-100 rounded-md p-4 mb-4">
				<h2><%#= pluralize(@contato.errors.count, "#{ "erro" if pluralize(@contato.errors.count == 1} #{ "erros" if pluralize(@contato.errors.count >= 2}") %><b>Impossível salvar o registro:</b></h2>
				<ul>
					<% @contato.errors.each do |error| %>
					<li><%= error.full_message.downcase %></li>
					<% end %>
				</ul>
			</div>
			<% end %>
		<% end %>

<% else %>
-
<% end %>



