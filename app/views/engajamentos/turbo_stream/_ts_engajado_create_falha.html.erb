<% if @engajamento.lideranca?(current_usuario) %>






<% if @pessoa.save %>
<%= turbo_stream.update "modal" do %><% end %>


<% else %>


		<%= turbo_stream.update "modal" do %>
		<div class="fixed z-20 inset-0 overflow-y-auto" aria-labelledby="modal-title" role="dialog" aria-modal="true">
			<div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
				<div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity" aria-hidden="true"></div>
				<span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
				<div class="inline-block align-bottom bg-white rounded-lg px-4 pt-5 pb-4 text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full sm:p-6">

					<%= render partial: "engajamentos/partials/form/engajado_novo" %>
					<%= turbo_stream.update "flash" do %>
					<% if @pessoa.errors.any? %>
					<div id="error_explanation" class="bg-red-100 rounded-md p-4 mb-4">
						<h2><%#= pluralize(@pessoa.errors.count, "#{ "erro" if pluralize(@pessoa.errors.count == 1} #{ "erros" if pluralize(@pessoa.errors.count >= 2}") %><b>Impossível salvar o registro:</b></h2>
						<ul>
							<% @pessoa.errors.each do |error| %>
							<li><%= error.full_message %></li>
							<% end %>
						</ul>
					</div>
					<% end %>
					<% end %>
				</div>
			</div>
		</div>
		<% end %>



<% end %>












<% end %>



